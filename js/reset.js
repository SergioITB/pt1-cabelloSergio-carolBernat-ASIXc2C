// Obtener el botón de reset
const resetBtn = document.getElementById('reset');

// Event listener para recargar la página
resetBtn.addEventListener('click', function() {
    location.reload(); // Recarga la página
});
